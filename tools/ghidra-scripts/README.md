# Ghidra Scripts

Shared Ghidra automation scripts for ROM analysis across all games in
the monorepo. Console-agnostic where possible; console-specific scripts
should be clearly labeled.

## Usage

Load these scripts via Ghidra's Script Manager or run them headless with
`analyzeHeadless`.
